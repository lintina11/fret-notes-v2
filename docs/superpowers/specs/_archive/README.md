# Archived design specs (history only)

These per-feature specs (dated **2026-06-18 → 2026-08-19**) were consolidated into
[`../00-design-reference.md`](../00-design-reference.md) (English) /
[`../00-design-reference-zh.md`](../00-design-reference-zh.md) (中文) on
**2026-08-25**.

Where a later spec re-discussed or changed a decision, the **newer** decision won
and is what the master reference records:

- `06-18` Fretboard grid → superseded by `06-22` SVG chord diagram
- `06-24` capo clear-on-raise → superseded by `06-25` relative transpose
- `07-14` note-map layout + `showOffOctave` default → superseded by `07-15`
- `08-19` audio synthesis engine → superseded by the **sampler migration**

**This folder is Claude-ignored** (see `/.claudeignore`). Do not treat anything
here as current design — read the master reference instead.

The audio **sampler migration** (`2026-08-20-audio-sampler-migration-design.md`)
is *not* archived here: it is the current, pending audio spec and stays live in
`docs/superpowers/specs/`.
